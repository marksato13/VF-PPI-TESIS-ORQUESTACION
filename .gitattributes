# Finales de linea. Anadido el 2026-09-17: al commitear desde Windows,
# core.autocrlf reescribe los finales de linea. En el repositorio del producto
# normalizo a LF dos CSV que se publicaron en CRLF y rompio sus hashes.
* text=auto eol=lf

# Los documentos binarios no se tocan.
*.docx binary
*.xlsx binary
*.pdf  binary
*.png  binary
*.jpg  binary
